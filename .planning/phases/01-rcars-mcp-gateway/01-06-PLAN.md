---
phase: 01-rcars-mcp-gateway
plan: 06
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - skills-plugin/skills/intake/SKILL.md
autonomous: true
requirements:
  - RCARS-05
user_setup: []

must_haves:
  truths:
    - "Intake skill uses ph_rcars_query MCP tool instead of broken curl call"
    - "Intake skill checks MCP tool availability before attempting vetting"
    - "Intake skill offers to skip vetting when MCP tool is unavailable"
    - "Intake skill works end-to-end for both deployment modes (onboarded and self-published)"
  artifacts:
    - path: "skills-plugin/skills/intake/SKILL.md"
      provides: "Updated vetting phase using MCP tool reference"
      contains: "ph_rcars_query"
  key_links:
    - from: "skills/intake/SKILL.md"
      to: "ph_rcars_query MCP tool"
      via: "MCP tool reference in skill instructions"
      pattern: "ph_rcars_query"
---

<objective>
Replace the broken curl call in the intake skill's vetting phase with the ph_rcars_query MCP tool reference.

Purpose: The intake skill currently has a broken `curl` call to RCARS `/recommend` (an endpoint that does not exist in RCARS v2). This plan replaces it with a reference to the `ph_rcars_query` MCP tool, which handles advisor job submission, polling, and result formatting. The skill checks MCP tool availability first and offers graceful degradation when unavailable.

Output: Updated SKILL.md with MCP-based vetting. Committed to the skills-plugin submodule via the main dev repo per D-10.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-rcars-mcp-gateway/01-RESEARCH.md
@.planning/phases/01-rcars-mcp-gateway/01-PATTERNS.md

<interfaces>
<!-- ph_rcars_query MCP tool contract (from Plan 04) -->
```
ph_rcars_query(query: str) -> dict
  Submits advisor query, polls until complete, returns:
  - On success: {"status": "completed", "result": {"matches": [...]}}
  - On timeout: {"status": "timeout", "error": "...", "result": null}
  - On RCARS down: {"error": "...", "status": "unavailable"}
```

<!-- Current broken vetting (SKILL.md lines 216-268, verified from PATTERNS.md) -->
```markdown
## Phase 2: Vetting (RCARS)

### Check RCARS Availability
1. **Read integrations.rcars_api from manifest**
   - If URL present: proceed to API call
   - If null/empty: ask user
2. **If RCARS unavailable:**
   - Ask: "Do you have an RCARS API endpoint URL, or should we skip vetting?"
   - If skip: set `lifecycle.phases.vetting.status: skipped` in manifest

### Call RCARS API
**Make API call:**
```bash
curl -s -X POST "${RCARS_API}/recommend" \
  -H "Content-Type: application/json" \
  -d '{"query": "<learning objectives + topics + products>", "limit": 10}'
```
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace broken vetting section with MCP tool reference</name>
  <files>
    /Users/nstephan/devel/publishing-house/rhdp-publishing-house/skills-plugin/skills/intake/SKILL.md
  </files>
  <read_first>
    /Users/nstephan/devel/publishing-house/rhdp-publishing-house/skills-plugin/skills/intake/SKILL.md
  </read_first>
  <action>
Find the "Phase 2: Vetting (RCARS)" section in SKILL.md (around lines 216-268). Replace the entire section (from `## Phase 2: Vetting (RCARS)` through the end of the RCARS results handling) with the MCP tool-based approach.

**Replace with this content:**

```markdown
## Phase 2: Vetting (RCARS)

### Check MCP Tool Availability

1. **Check if the `ph_rcars_query` MCP tool is available**
   - If available: proceed to vetting
   - If unavailable (no MCP server configured, no API key, or tool not found):
     - Inform the user: "RCARS vetting requires the Publishing House MCP server. The MCP server provides access to RCARS content advisory tools for overlap detection."
     - Ask: "Should we skip vetting for now? You can run vetting later once MCP access is configured."
     - If skip: set `lifecycle.phases.vetting.status: skipped` and `lifecycle.phases.vetting.skip_reason: "MCP unavailable"` in manifest
     - Proceed to Phase 3

### Run Vetting Query

**Build query from spec:**
Combine these elements from the project specification into a single natural language query:
- Learning objectives (what the user will learn)
- Topics and technologies covered
- Products featured (e.g., OpenShift, AAP, RHEL)
- Target audience level (beginner, intermediate, advanced)
- Deployment mode context (e.g., "workshop for live events" or "self-paced demo")

Example query: "Workshop teaching OpenShift GitOps with ArgoCD for intermediate users, covering deployment strategies, rollbacks, and multi-cluster management"

**Call MCP tool:**
Use the `ph_rcars_query` tool with the combined query string. The tool handles:
- Submitting the advisor query to RCARS
- Polling for completion (up to 120 seconds)
- Returning structured results with matching catalog items and relevance tiers

**Handle results:**

1. **If result status is "completed":**
   - Parse the matching catalog items from `result.result`
   - Each match includes: CI name, display name, stage (prod/dev/event), relevance tier, and rationale
   - Classify the vetting outcome:
     - **Green tier matches:** Existing content is closely related -- potential overlap. Recommend reviewing these CIs before proceeding.
     - **Yellow tier matches:** Some overlap exists -- may complement rather than duplicate. Note for the user's awareness.
     - **White/no matches:** No significant overlap found -- clear to proceed.

2. **If result status is "unavailable" or "timeout":**
   - Log the error from `result.error`
   - Inform the user: "RCARS vetting could not complete: {error}. This does not block intake -- you can retry vetting later."
   - Set `lifecycle.phases.vetting.status: error` and `lifecycle.phases.vetting.error: "{error}"` in manifest
   - Proceed to Phase 3

3. **If result status is "failed":**
   - Log the error from `result.error`
   - Inform the user of the failure and proceed as above

### Write Vetting Report

Write the vetting results to `publishing-house/reviews/rcars-vetting.md`:
- Query used
- Number of matches found
- For each match: CI name, display name, stage, tier, rationale summary
- Overall recommendation (proceed / review overlaps / revise scope)

### Update Manifest

Set vetting status in manifest:
- `lifecycle.phases.vetting.status`: `approved` (no significant overlap), `review` (overlaps found, user acknowledged), `skipped`, or `error`
- `lifecycle.phases.vetting.query`: the query string used
- `lifecycle.phases.vetting.matches_count`: number of matches found
- `lifecycle.phases.vetting.completed_at`: ISO timestamp
```

**Key differences from the old version:**
1. Checks MCP tool availability instead of `integrations.rcars_api` manifest field
2. Uses `ph_rcars_query` MCP tool instead of `curl -X POST /recommend`
3. Handles structured result tiers (green/yellow/white) from advisor
4. Handles timeout, unavailable, and failed states gracefully
5. Sets more detailed manifest fields (skip_reason, error, query, matches_count)

**Preserve everything else in SKILL.md** -- only the Phase 2 vetting section changes. Phase 1 (spec gathering), Phase 3 (design), and all other sections remain unchanged.

Per D-10: This file is in the skills-plugin submodule. Commit through the main dev repo. After committing, the user may need to pull the submodule in local clones.
  </action>
  <verify>
    <automated>cd /Users/nstephan/devel/publishing-house/rhdp-publishing-house && grep -c "ph_rcars_query" skills-plugin/skills/intake/SKILL.md && grep -c "curl.*recommend" skills-plugin/skills/intake/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains `ph_rcars_query` (at least 3 references -- availability check, tool call, result handling)
    - SKILL.md does NOT contain `curl -s -X POST` or `/recommend` endpoint reference (broken code removed)
    - SKILL.md does NOT contain `integrations.rcars_api` manifest field reference (old availability check removed)
    - SKILL.md contains `lifecycle.phases.vetting.status: skipped` for MCP unavailable case
    - SKILL.md contains `lifecycle.phases.vetting.status: error` for RCARS failure case
    - SKILL.md contains `lifecycle.phases.vetting.status: approved` for successful vetting
    - SKILL.md contains `publishing-house/reviews/rcars-vetting.md` report path
    - SKILL.md contains "MCP server" in the unavailability message
    - grep for `curl.*recommend` returns 0 (broken curl completely removed)
    - Phase 1 and Phase 3 sections of SKILL.md are unchanged
  </acceptance_criteria>
  <done>Intake skill vetting uses ph_rcars_query MCP tool. Broken curl removed. Graceful degradation when MCP unavailable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude Code -> MCP tool | Claude Code invokes ph_rcars_query via MCP transport. Auth handled by ApiKeyAuth middleware (Plan 03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23 | Injection | SKILL.md query construction | accept | Query is built from user-provided spec content. The MCP tool passes it as a string parameter to RCARS advisor. RCARS advisor handles its own input sanitization. Skill constructs the query from structured spec fields, not raw user input. |
| T-01-24 | Denial of Service | SKILL.md vetting timeout | mitigate | MCP tool has 120s timeout. Skill handles timeout gracefully (logs error, proceeds to Phase 3). No hang or crash. |
</threat_model>

<verification>
1. `grep -c "ph_rcars_query" skills-plugin/skills/intake/SKILL.md` returns >= 3
2. `grep -c "curl.*recommend" skills-plugin/skills/intake/SKILL.md` returns 0
3. Manual verification: run intake skill on a test project to confirm MCP tool reference is picked up
</verification>

<success_criteria>
- Intake SKILL.md Phase 2 uses ph_rcars_query MCP tool instead of broken curl
- Availability check looks for MCP tool, not manifest URL field
- Graceful degradation: skip option when MCP unavailable, error handling for timeouts
- Vetting report written to reviews/rcars-vetting.md
- Manifest updated with detailed vetting status
- All other SKILL.md phases unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-rcars-mcp-gateway/01-06-SUMMARY.md`
</output>
