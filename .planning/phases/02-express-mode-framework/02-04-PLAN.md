---
phase: 02-express-mode-framework
plan: 04
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified:
  - skills-plugin/skills/intake/SKILL.md
autonomous: true
requirements:
  - EXPRESS-09

must_haves:
  truths:
    - "Intake presents three deployment modes after vetting: onboarded, self-published, express (per D-11)"
    - "Express mode runs a second RCARS base-finding query for infrastructure-focused CI discovery (per D-12)"
    - "Express flow stores intake data in portal DB only via ph_store_intake_results MCP tool (per D-04)"
    - "Express flow dead-ends at the environment gate with clear instructions (per D-13)"
    - "When MCP is unavailable, only two modes are presented and express is disabled with explanation (per Pitfall 6)"
    - "For onboarded and self-published modes, intake also stores results in portal DB for session continuity"
    - "After manifest writes in onboarded/self-published, intake calls ph_sync_manifest"
  artifacts:
    - path: "skills-plugin/skills/intake/SKILL.md"
      provides: "Intake skill with three-mode routing, express dead-end, session continuity"
      contains: "express"
      min_lines: 300
  key_links:
    - from: "intake SKILL.md"
      to: "ph_store_intake_results MCP tool"
      via: "MCP tool call after intake completes"
      pattern: "ph_store_intake_results"
    - from: "intake SKILL.md"
      to: "ph_rcars_query MCP tool"
      via: "second RCARS query for express base-finding"
      pattern: "ph_rcars_query"
    - from: "intake SKILL.md"
      to: "ph_record_express_run MCP tool"
      via: "records express run metric"
      pattern: "ph_record_express_run"
    - from: "intake SKILL.md"
      to: "ph_sync_manifest MCP tool"
      via: "syncs manifest to portal after writes"
      pattern: "ph_sync_manifest"
---

<objective>
Update the intake SKILL.md to add express mode as a third deployment option, three-mode routing after vetting, express RCARS base-finding query, portal DB storage for all modes, express dead-end at environment gate, and manifest sync instructions.

Purpose: This completes the intake routing for express mode (D-11, D-12, D-13) and adds session continuity for all modes (D-04, D-08, D-09). Express mode creates a lightweight path for users who need "a working environment by Thursday" -- intake, base CI identification, then handoff to manual environment provisioning. The intake skill also stores results in the portal for all modes, enabling session continuity across Claude Code restarts.
Output: Updated intake SKILL.md with three-mode routing, express flow, and session continuity hooks.
</objective>

<execution_context>
@/Users/nstephan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/nstephan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-express-mode-framework/02-CONTEXT.md
@.planning/phases/02-express-mode-framework/02-RESEARCH.md
@.planning/phases/02-express-mode-framework/02-PATTERNS.md
@.planning/phases/02-express-mode-framework/02-03-SUMMARY.md

<interfaces>
<!-- MCP tools the intake skill will call. -->

Session continuity MCP tools (from Plan 02):
```
ph_store_intake_results(owner_email: str, mode: str, intake_data: dict, project_name: str | None = None) -> dict
    # Returns {session_id, owner_email, mode, created_at}

ph_record_express_run(owner_email: str, base_ci: str | None = None, automated: bool = False) -> dict
    # Returns {id, owner_email, base_ci, automated, created_at}

ph_sync_manifest(project_id: str, manifest_yaml: str) -> dict
    # Returns {manifest_id, project_id, synced_at}
```

RCARS MCP tools (from Phase 1):
```
ph_rcars_query(query: str) -> dict
    # Submits advisor query, polls for completion, returns structured results
    # Results contain: status, result.matches[].ci_name, result.matches[].tier, etc.
```

Orchestrator context (from Plan 03):
- User email is already resolved by the orchestrator before dispatching intake
- MCP availability is already checked by the orchestrator
- The orchestrator passes MCP availability status to dispatched skills
- Manifest Sync Rule is defined in orchestrator -- intake follows the same pattern

Manifest deployment_mode values:
- "rhdp_published" (onboarded)
- "self_published" (self-published)
- "express" (new in this phase)
```

Current intake SKILL.md structure (preserved sections):
- Phase 1: Intake (Smart Intake, Detect Entry Path, Path A/B, Repo Setup, Spec Output Rules)
- Phase 2: Vetting (RCARS)
- Phase 3: Spec Refinement
- After Refinement: Hand Back
- Key Behavioral Notes
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update intake SKILL.md with three-mode routing and express flow</name>
  <files>skills-plugin/skills/intake/SKILL.md</files>
  <read_first>
    skills-plugin/skills/intake/SKILL.md
    .planning/phases/02-express-mode-framework/02-CONTEXT.md
    .planning/phases/02-express-mode-framework/02-RESEARCH.md
  </read_first>
  <action>
Update `skills-plugin/skills/intake/SKILL.md` with the following changes. Preserve ALL existing content that is not explicitly modified below.

**A. Add "MCP Context" section after "## Before Starting" (before Phase 1):**

```markdown
## MCP Context

The orchestrator resolves MCP availability and user email before dispatching intake. These are available in session context:

- **User email:** The user's Red Hat email (resolved by orchestrator per User Email Identification)
- **MCP available:** Whether portal MCP tools are accessible this session

If MCP is unavailable:
- Express mode is NOT offered during mode selection (express requires portal DB)
- Session continuity storage is skipped (intake results only saved locally)
- Intake proceeds normally for onboarded and self-published modes
```

**B. Add "Deployment Mode Selection" section AFTER the Phase 2 (Vetting) section and BEFORE Phase 3 (Spec Refinement):**

This is a NEW phase inserted between vetting and spec refinement. It replaces the current single deployment_mode question in the intake conversational flow.

```markdown
## Deployment Mode Selection

After vetting completes (or is skipped), present the deployment mode choice. This determines the project's path through the rest of the Publishing House lifecycle.

### Check MCP Before Presenting Options

If MCP is unavailable, present only two options and explain why:

> Choose your deployment mode:
>
> 1. **Onboarded** (rhdp_published) -- Full RHDP pipeline: AgnosticV catalog, code reviews, published in RHDP
> 2. **Self-published** (self_published) -- GitOps repo, Field Source CI, self-managed publishing
>
> *Express mode is not available this session -- it requires a portal connection for intake data storage. Configure your API key to enable it.*

If MCP is available, present all three options:

> Choose your deployment mode:
>
> 1. **Onboarded** (rhdp_published) -- Full RHDP pipeline: AgnosticV catalog, code reviews, published in RHDP
> 2. **Self-published** (self_published) -- GitOps repo, Field Source CI, self-managed publishing
> 3. **Express** -- Disposable demo environment. PH helps you find a base, you order a Babylon environment, customize it, and walk away. No content repo, no lifecycle tracking.

Do NOT steer the user toward any mode. Present options neutrally. User selects.

### If User Selects Express (Mode 3)

Express mode follows a separate, shorter flow:

**Step E1: Express RCARS Base-Finding Query**

Run a second RCARS query focused on infrastructure rather than content overlap:

Build query from the intake data gathered so far:
- Products and technologies mentioned
- Infrastructure requirements (cluster type, operators needed)
- Learner environment description
- Frame the query as: "Find existing catalog items that provide infrastructure similar to: [products/operators/environment description]. Looking for base environments that can be customized, not content overlap."

Call `ph_rcars_query` with this infrastructure-focused query.

**Handle results:**
- If matches found: Present the top 3-5 matches as potential base CIs:
  > Based on your requirements, these existing catalog items could serve as your base environment:
  >
  > 1. **agd_v2/ocp-cnv** (OCP with CNV) -- Provides OCP 4.x cluster with CNV operator
  > 2. **agd_v2/ocp-gpu** (OCP with GPU) -- Provides OCP cluster with GPU nodes
  >
  > Which one looks closest to what you need? Or describe what's different about your needs.

- If no matches: Inform the user:
  > RCARS did not find a close infrastructure match. You may need to work with the platform team to identify or create an appropriate base catalog item.

- Record the selected base CI (or "none found") for the express metric.

**Note on quality:** This base-finding query relies on content analysis as a proxy for infrastructure matching. Quality is limited until RCARS gets infrastructure-aware catalog metadata (backlogged in both PH and RCARS backlogs per D-03).

**Step E2: Store Express Intake Data in Portal DB**

Call `ph_store_intake_results` MCP tool:
```
ph_store_intake_results(
    owner_email="<user_email>",
    mode="express",
    intake_data={
        "project": {
            "name": "<project_name or user description>",
            "type": "express",
            "deployment_mode": "express"
        },
        "requirements": {
            "products": [...],
            "environment": "...",
            "base_ci": "<selected CI or null>"
        },
        "vetting": {
            "status": "<vetting result>",
            "base_finding": {
                "query": "<query string>",
                "matches": [...]
            }
        }
    },
    project_name="<project name>"
)
```

**Step E3: Record Express Metric**

Call `ph_record_express_run` MCP tool:
```
ph_record_express_run(
    owner_email="<user_email>",
    base_ci="<selected CI name or null>",
    automated=false
)
```
The `automated` field is always `false` until Babylon ordering automation is built.

**Step E4: Dead-End at Environment Gate**

Present the environment gate message and stop:

> **Express intake complete.**
>
> Your base CI is: **<selected CI name>** (or "No base CI identified -- work with the platform team")
>
> **Next steps (manual):**
> 1. Order a Babylon environment based on the base CI above
> 2. Once the environment is provisioned and you have access, come back and we can help you customize it
>
> Your intake data is saved in the portal (session ID: <session_id>). You can resume this express project later by running `/rhdp-publishing-house` -- the orchestrator will find your saved session.
>
> *The express skill (automated cluster customization) is not yet built. For now, customization is manual.*

Do NOT write a local manifest for express projects. Do NOT create a git repo. Do NOT proceed to spec refinement or any further phases. Express intake ends here.

### If User Selects Onboarded (Mode 1) or Self-Published (Mode 2)

Proceed with the existing flow:

1. Set `project.deployment_mode` in the manifest to `rhdp_published` or `self_published`
2. Continue to Phase 3 (Spec Refinement) as before

**Session Continuity Addition:**
After writing the manifest for onboarded/self-published modes (at the end of Phase 1 or after mode selection), also store intake results in the portal:

Call `ph_store_intake_results` MCP tool:
```
ph_store_intake_results(
    owner_email="<user_email>",
    mode="<onboarded or self_published>",
    intake_data=<full intake data dict matching the manifest shape>,
    project_name="<project.name from manifest>"
)
```

If MCP is unavailable, skip this step silently (intake data is still in the local manifest).

**Manifest Sync Addition:**
After every manifest write during intake (setting intake status, adding artifacts, etc.), call `ph_sync_manifest` per the Manifest Sync Rule defined in the orchestrator SKILL.md. If MCP is unavailable, skip silently.
```

**C. Update the existing "Deployment mode" question in the intake conversational flow (Path B, under "Extract and Follow Up"):**

The current intake flow asks about deployment mode as one of the required fields. Change the description from:
```
- **Deployment mode** -- RHDP Published (`rhdp_published`: full pipeline, AgnosticV catalog, reviews, published in RHDP) or Self-Published (`self_published`: GitOps repo, generic Field Source CI, faster but self-managed).
```

To:
```
- **Deployment mode** -- Captured during Deployment Mode Selection (after vetting). Do NOT ask about deployment mode during the intake interview. The mode selection is a separate step presented after vetting.
```

Also update the Path A (Full Spec Provided) section: if the existing spec mentions deployment mode, capture it but do not finalize -- the Deployment Mode Selection step after vetting will present the full choice including express.

**D. Update the "Manifest update after Intake" YAML example:**

Add `express` to the valid deployment_mode values comment:
```yaml
  deployment_mode: "rhdp_published"  # rhdp_published | self_published | express
```

**E. Update the Vetting section -- add express awareness:**

After the "### Update Manifest" section in Phase 2 (Vetting), add a transition note:
```markdown
After vetting, proceed to **Deployment Mode Selection** to choose the project's deployment path.
```

**F. Preserve ALL other sections unchanged:**
- Frontmatter (name, description, context, model)
- Smart Intake
- Detect Entry Path
- Path A: Full Spec Provided (except deployment mode capture note)
- Path B: Idea (except deployment mode question removal)
- Repo Setup
- Spec Output Rules
- Phase 2: Vetting (RCARS) (except transition note)
- Phase 3: Spec Refinement
- After Refinement: Hand Back
- Key Behavioral Notes
  </action>
  <verify>
    <automated>grep -c "express" /Users/nstephan/devel/publishing-house/rhdp-publishing-house/skills-plugin/skills/intake/SKILL.md && grep -c "ph_store_intake_results" /Users/nstephan/devel/publishing-house/rhdp-publishing-house/skills-plugin/skills/intake/SKILL.md && grep -c "ph_record_express_run" /Users/nstephan/devel/publishing-house/rhdp-publishing-house/skills-plugin/skills/intake/SKILL.md && grep -c "Dead-End" /Users/nstephan/devel/publishing-house/rhdp-publishing-house/skills-plugin/skills/intake/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md contains "## MCP Context" section explaining MCP availability and user email
    - SKILL.md contains "## Deployment Mode Selection" section with three modes: Onboarded, Self-published, Express
    - SKILL.md contains MCP unavailability check that presents only two modes when MCP is unavailable
    - SKILL.md contains "### If User Selects Express (Mode 3)" with steps E1 through E4
    - SKILL.md contains `ph_rcars_query` call for base-finding with infrastructure-focused query
    - SKILL.md contains `ph_store_intake_results` calls for both express and onboarded/self-published modes
    - SKILL.md contains `ph_record_express_run` call with base_ci and automated=false
    - SKILL.md contains "Dead-End at Environment Gate" with the environment gate message
    - SKILL.md contains `deployment_mode: "rhdp_published"  # rhdp_published | self_published | express` in the manifest example
    - SKILL.md contains `ph_sync_manifest` reference for manifest sync after writes
    - SKILL.md preserves frontmatter with `name: rhdp-publishing-house:intake`
    - SKILL.md preserves Phase 2 (Vetting) section with RCARS query flow
    - SKILL.md preserves Phase 3 (Spec Refinement) section
    - SKILL.md preserves "Key Behavioral Notes" section
    - `grep -c "express" SKILL.md` returns at least 15
    - `grep -c "ph_store_intake_results" SKILL.md` returns at least 3
  </acceptance_criteria>
  <done>Intake SKILL.md updated with three-mode routing, express base-finding RCARS query, portal DB storage for all modes, express dead-end at environment gate, and manifest sync hooks. All existing intake behavior preserved for onboarded and self-published modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input to intake skill | User provides project requirements, selects deployment mode. Skill processes locally. |
| Intake skill to portal MCP | Stores intake data via MCP tools. Protected by API key auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Tampering | Express intake_data stored in portal | mitigate | intake_data is constructed by the skill from validated user input, not passed through raw. MCP tool validates with Pydantic schema (from Plan 02). |
| T-02-13 | Information Disclosure | Express base CI recommendations | accept | RCARS results are filtered by the same API key auth. Base CI names are not sensitive (public catalog items). |
| T-02-14 | Denial of Service | Express mode blocking without MCP | mitigate | Pitfall 6 addressed: MCP availability checked before presenting mode options. Express not offered when MCP unavailable. User sees clear explanation. |
</threat_model>

<verification>
- `grep "Deployment Mode Selection" skills-plugin/skills/intake/SKILL.md` returns match
- `grep "Dead-End at Environment Gate" skills-plugin/skills/intake/SKILL.md` returns match
- `grep "ph_store_intake_results" skills-plugin/skills/intake/SKILL.md` returns multiple matches
- `grep "ph_record_express_run" skills-plugin/skills/intake/SKILL.md` returns match
- `grep "express" skills-plugin/skills/intake/SKILL.md` returns many matches
- SKILL.md still contains all original sections (Smart Intake, Detect Entry Path, Vetting, Spec Refinement, etc.)
</verification>

<success_criteria>
Intake SKILL.md presents three deployment modes after vetting (onboarded, self-published, express). Express mode runs a second RCARS query for base CI, stores intake in portal DB only, records express metric, and dead-ends at environment gate. When MCP is unavailable, only two modes are offered. All modes store intake results in portal for session continuity. Manifest sync called after writes. All existing intake behavior for onboarded and self-published modes preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/02-express-mode-framework/02-04-SUMMARY.md`
</output>
